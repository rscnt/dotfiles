if has("gui_running")
    set guioptions -=m
    set guioptions -=T
    set guioptions -=r
    set guifont=Inconsolata\ for\ Powerline\ Medium\ 10 
    if has("gui_macvim")
        set guifont=Sauce\ Code\ Powerline:h11
    endif
endif
